# To run: conda env create -f environment-lnx-cuda.yml
name: pytorchenv
channels:
    - conda-forge
dependencies:
    - python=3.9
    - pip
    - Pillow
    - matplotlib
    - numpy
    - pandas
    - cudatoolkit=11.6
    - pip:
        - torch
        - torchvision
        - kaggle
        - pydicom
        - opencv-python
        - nibabel
